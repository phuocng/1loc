---
layout: layouts/base.njk
title: Favorite JavaScript utilities in single line of code
eleventyExcludeFromCollections: true
---

<div class="container">
    <div class="hero">
        <h1 class="hero__heading">Favorite JavaScript utilities in single line of code! No more!</h1>
        <a class="hero__button" href="https://github.com/phuocng/1loc">Star me on GitHub · 6k★</a>
    </div>

    {% for cat in collections.categories %}
    {% set posts = collections.groupByCategories[cat] %}
    <div class="category">
        <h2 class="category__name">{{ cat }}</h2>
        <div class="category__posts">            
            {%- for post in posts | head(8) -%}
            <div class="card__item">
                <a class="card__link" href="{{ post.url }}">{{ post.data.title }}</a>
            </div>
            {%- endfor -%}
        </div>
        <a class="category__link" href="/{{ cat | lower | slug }}">See all {{ posts | length }} helpers</a>
    </div>
    {% endfor %}

    {% include "follow.njk" %}
</div>