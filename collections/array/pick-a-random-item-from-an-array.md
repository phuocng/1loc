~~~ javascript
const pickRandom = arr => arr[Math.floor(Math.random() * arr.length)];

// pickRandom([1, 2, 3, 4]) returns one of 1, 2, 3, or 4
// pickRandom(['hello', 'hello', 'hello']) returns 'hello'
~~~
