~~~ javascript
const removeLast = arr => Array.isArray(arr) ? arr.pop() : undefined;
// globalArr = [1,2,3,4,5,6];
// getLast(globalArr); // it returns deleted item (6 in the case)
// globalArr is [1,2,3,4,5];
~~~
